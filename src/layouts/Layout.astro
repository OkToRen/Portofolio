---
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Darren Oliver Putra Haryanto - Portfolio" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300..600&family=Inter+Tight:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap"
			rel="stylesheet"
		/>
		<title>{title}</title>
	</head>
	<body class="bg-background text-primary font-sans antialiased selection:bg-accent selection:text-white transition-colors duration-300">
		<div class="fixed top-6 right-6 flex gap-3 z-50">
			<!-- Collapsible Menu Button -->
			<div class="relative">
				<button id="menu-toggle" data-magnetic class="p-3 rounded-lg bg-surface border border-border hover:border-accent transition-all" aria-label="Toggle menu">
					<svg id="menu-icon-open" class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
					</svg>
					<svg id="menu-icon-close" class="w-5 h-5 text-accent hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
					</svg>
				</button>
				
				<!-- Dropdown Menu -->
				<div id="dropdown-menu" class="absolute top-full right-0 mt-2 w-48 rounded-lg bg-surface border border-border shadow-lg opacity-0 invisible transition-all duration-300 transform scale-95 origin-top-right">
					<nav class="py-2">
						<a href="/#content" class="block px-4 py-2.5 text-sm text-primary hover:text-accent hover:bg-background transition-colors">
							<div class="flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
								</svg>
								<span>Content</span>
							</div>
						</a>
						<a href="/projects" class="block px-4 py-2.5 text-sm text-primary hover:text-accent hover:bg-background transition-colors">
							<div class="flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path>
								</svg>
								<span>Projects</span>
							</div>
						</a>
						<!-- <a href="/#about" class="block px-4 py-2.5 text-sm text-primary hover:text-accent hover:bg-background transition-colors">
							<div class="flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
								</svg>
								<span>About</span>
							</div>
						</a>
						<a href="/#experience" class="block px-4 py-2.5 text-sm text-primary hover:text-accent hover:bg-background transition-colors">
							<div class="flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
								</svg>
								<span>Experience</span>
							</div>
						</a>
						<a href="/#skills" class="block px-4 py-2.5 text-sm text-primary hover:text-accent hover:bg-background transition-colors">
							<div class="flex items-center gap-2">
								<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
								</svg>
								<span>Skills</span>
							</div>
						</a> -->
					</nav>
				</div>
			</div>
			
			<a href="/cv.pdf" download="Darren_Haryanto_CV.pdf"
			   id="cv-download"
			   data-magnetic
			   class="p-3 rounded-lg bg-surface border border-border hover:border-accent transition-all flex items-center gap-2 text-sm font-medium text-primary hover:text-accent"
			   aria-label="Download CV">
				<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
				</svg>
				<span class="hidden sm:inline">CV</span>
			</a>
			
			<button id="theme-toggle" data-magnetic class="p-3 rounded-lg bg-surface border border-border hover:border-accent transition-all" aria-label="Toggle theme">
				<svg id="theme-icon-dark" class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
				</svg>
				<svg id="theme-icon-light" class="w-5 h-5 text-accent hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
				</svg>
			</button>
		</div>
		<main class="max-w-4xl mx-auto px-6 py-20">
			<slot />
		</main>

		<!-- Pointer-effect layers (fine pointer only; styled in global.css) -->
		<div class="cursor-spotlight" aria-hidden="true"></div>
		<div class="cursor-ring" aria-hidden="true"><span class="cursor-ring__inner"></span></div>

		<!-- Lightbox overlay for screenshot galleries -->
		<div
			id="lightbox"
			class="fixed inset-0 z-[9998] hidden items-center justify-center bg-background/95 backdrop-blur-sm p-6 cursor-zoom-out"
			aria-hidden="true"
		>
			<button id="lightbox-prev" type="button" aria-label="Previous screenshot" class="absolute left-3 sm:left-6 top-1/2 -translate-y-1/2 h-11 w-11 flex items-center justify-center rounded-full bg-surface/80 border border-border text-primary hover:border-accent hover:text-accent transition cursor-pointer">
				<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
			</button>
			<img id="lightbox-img" src="" alt="" class="max-h-[90vh] max-w-[80vw] rounded-lg border border-border object-contain" />
			<button id="lightbox-next" type="button" aria-label="Next screenshot" class="absolute right-3 sm:right-6 top-1/2 -translate-y-1/2 h-11 w-11 flex items-center justify-center rounded-full bg-surface/80 border border-border text-primary hover:border-accent hover:text-accent transition cursor-pointer">
				<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
			</button>
		</div>
	</body>
</html>

<script>
	const theme = (() => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		if (window.matchMedia('(prefers-color-scheme: light)').matches) {
			return 'light';
		}
		return 'dark';
	})();

	if (theme === 'light') {
		document.documentElement.classList.add('light');
	} else {
		document.documentElement.classList.remove('light');
	}

	window.localStorage.setItem('theme', theme as string);

	const handleToggleClick = (event: MouseEvent) => {
		const element = document.documentElement;
		const button = event.currentTarget as HTMLElement;

		// Get the position of the button for the circular reveal animation
		const rect = button.getBoundingClientRect();
		const x = rect.left + rect.width / 2;
		const y = rect.top + rect.height / 2;

		// Calculate the maximum radius needed to cover the entire screen
		const endRadius = Math.hypot(
			Math.max(x, window.innerWidth - x),
			Math.max(y, window.innerHeight - y)
		);

		// Toggle the theme with a circular reveal animation
		const isDarkNow = !element.classList.contains('light');
		const newTheme = isDarkNow ? 'light' : 'dark';

		// Check if the browser supports View Transitions API
		if (!document.startViewTransition) {
			// Fallback for browsers that don't support View Transitions
			element.classList.toggle('light');
			localStorage.setItem('theme', newTheme);
			updateThemeIcon(!isDarkNow);
			return;
		}

		// Use View Transitions API for smooth circular reveal
		const transition = document.startViewTransition(() => {
			element.classList.toggle('light');
			localStorage.setItem('theme', newTheme);
			updateThemeIcon(!isDarkNow);
		});

		// Animate the transition with a circular clip-path
		transition.ready.then(() => {
			const clipPath = [
				`circle(0px at ${x}px ${y}px)`,
				`circle(${endRadius}px at ${x}px ${y}px)`
			];

			document.documentElement.animate(
				{ clipPath: clipPath },
				{
					duration: 700,
					easing: 'ease-in-out',
					pseudoElement: '::view-transition-new(root)'
				}
			);
		});
	};

	const updateThemeIcon = (isDark: boolean) => {
		const darkIcon = document.getElementById('theme-icon-dark');
		const lightIcon = document.getElementById('theme-icon-light');
		if (isDark) {
			darkIcon?.classList.remove('hidden');
			lightIcon?.classList.add('hidden');
		} else {
			darkIcon?.classList.add('hidden');
			lightIcon?.classList.remove('hidden');
		}
	};

	document.getElementById('theme-toggle')?.addEventListener('click', handleToggleClick);

	// Set initial icon state
	const isDark = !document.documentElement.classList.contains('light');
	updateThemeIcon(isDark);

	// Menu toggle functionality
	const menuToggle = document.getElementById('menu-toggle');
	const menuIconOpen = document.getElementById('menu-icon-open');
	const menuIconClose = document.getElementById('menu-icon-close');
	const dropdownMenu = document.getElementById('dropdown-menu');
	let isMenuOpen = false;

	const toggleMenu = () => {
		isMenuOpen = !isMenuOpen;
		
		if (isMenuOpen) {
			// Open menu
			dropdownMenu?.classList.remove('opacity-0', 'invisible', 'scale-95');
			dropdownMenu?.classList.add('opacity-100', 'visible', 'scale-100');
			menuIconOpen?.classList.add('hidden');
			menuIconClose?.classList.remove('hidden');
		} else {
			// Close menu
			dropdownMenu?.classList.add('opacity-0', 'invisible', 'scale-95');
			dropdownMenu?.classList.remove('opacity-100', 'visible', 'scale-100');
			menuIconOpen?.classList.remove('hidden');
			menuIconClose?.classList.add('hidden');
		}
	};

	menuToggle?.addEventListener('click', toggleMenu);

	// Close menu when clicking outside
	document.addEventListener('click', (e) => {
		const target = e.target as HTMLElement;
		if (isMenuOpen && !menuToggle?.contains(target) && !dropdownMenu?.contains(target)) {
			toggleMenu();
		}
	});

	// Custom smooth scroll function with 800ms duration
	const  smoothScrollTo = (targetPosition: number, duration: number = 800) => {
		const startPosition = window.scrollY;
		const distance = targetPosition - startPosition;
		let startTime: number | null = null;

		const easeInOutCubic = (t: number): number => {
			return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;
		};

		const animation = (currentTime: number) => {
			if (startTime === null) startTime = currentTime;
			const timeElapsed = currentTime - startTime;
			const progress = Math.min(timeElapsed / duration, 1);
			const ease = easeInOutCubic(progress);

			window.scrollTo(0, startPosition + distance * ease);

			if (timeElapsed < duration) {
				requestAnimationFrame(animation);
			}
		};

		requestAnimationFrame(animation);
	};

	// Close menu when clicking a menu item
	dropdownMenu?.querySelectorAll('a').forEach(link => {
		link.addEventListener('click', (e) => {
			const href = link.getAttribute('href');
			
			// Handle anchor links with smooth scrolling
			if (href?.startsWith('/#')) {
				e.preventDefault();
				const targetId = href.substring(2); // Remove /#
				const targetElement = document.getElementById(targetId);
				
				if (targetElement) {
					const targetPosition = targetElement.getBoundingClientRect().top + window.scrollY;
					const offsetPosition = targetPosition - 100; // Account for scroll-padding-top
					
					smoothScrollTo(offsetPosition, 800);
				}
			}
			
			if (isMenuOpen) {
				toggleMenu();
			}
		});
	});

	// Respect the user's motion preference for all decorative interactions.
	const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	// Subtle pointer-driven tilt on cards marked [data-tilt].
	if (!prefersReducedMotion) {
		document.querySelectorAll<HTMLElement>('[data-tilt]').forEach((card) => {
			const MAX = 4; // degrees, restrained on purpose
			card.addEventListener('pointermove', (e) => {
				const r = card.getBoundingClientRect();
				const px = (e.clientX - r.left) / r.width - 0.5;
				const py = (e.clientY - r.top) / r.height - 0.5;
				card.style.transform = `perspective(900px) rotateX(${(-py * MAX).toFixed(2)}deg) rotateY(${(px * MAX).toFixed(2)}deg) translateY(-4px)`;
			});
			card.addEventListener('pointerleave', () => {
				card.style.transform = '';
			});
		});
	}

	// Pointer effects (fine pointer + motion-allowed only):
	//   4 · ring follows the cursor, reacts over interactive elements
	//   5 · faint glow pools under the pointer across #content only
	//   6 · control-cluster buttons lean toward the pointer
	const finePointer = window.matchMedia('(pointer: fine)').matches;

	if (finePointer && !prefersReducedMotion) {
		const ring = document.querySelector<HTMLElement>('.cursor-ring');
		const glow = document.querySelector<HTMLElement>('.cursor-spotlight');
		const magnets = Array.from(document.querySelectorAll<HTMLElement>('[data-magnetic]'));
		// The glow shows across the whole content field (margins included),
		// not just the centred text column — so gate on "past the hero"
		// rather than on the element under the pointer. Pages with no hero
		// (projects list/detail) are content top-to-bottom, so always on.
		const heroWrapper = document.querySelector('.hero-wrapper');

		let pointerX = window.innerWidth / 2;
		let pointerY = window.innerHeight / 2;
		let ringX = pointerX;
		let ringY = pointerY;
		let glowX = pointerX;
		let glowY = pointerY;

		const frame = () => {
			// Ring eases quickly; glow trails a touch slower for a liquid feel.
			ringX += (pointerX - ringX) * 0.2;
			ringY += (pointerY - ringY) * 0.2;
			glowX += (pointerX - glowX) * 0.12;
			glowY += (pointerY - glowY) * 0.12;
			if (ring) ring.style.transform = `translate(${ringX}px, ${ringY}px)`;
			if (glow) glow.style.transform = `translate(${glowX}px, ${glowY}px)`;
			requestAnimationFrame(frame);
		};

		window.addEventListener(
			'pointermove',
			(e) => {
				if (e.pointerType !== 'mouse') return;
				pointerX = e.clientX;
				pointerY = e.clientY;
				const el = e.target as HTMLElement;

				// 4 · ring — visible on move, accent + grown over interactive els
				if (ring) {
					ring.classList.add('is-visible');
					const interactive = el.closest('a, button, [data-tilt], input, label, [role="button"]');
					ring.classList.toggle('is-interactive', !!interactive);
				}

				// 5 · glow — across the content field, hidden while over the hero
				if (glow) {
					const pastHero = !heroWrapper || window.scrollY > window.innerHeight * 0.6;
					glow.classList.toggle('is-visible', pastHero);
				}

				// 6 · magnetic pull within proximity
				for (const magnet of magnets) {
					const r = magnet.getBoundingClientRect();
					const dx = e.clientX - (r.left + r.width / 2);
					const dy = e.clientY - (r.top + r.height / 2);
					const radius = Math.max(r.width, r.height) / 2 + 36;
					if (Math.hypot(dx, dy) < radius) {
						magnet.style.transform = `translate(${(dx * 0.28).toFixed(2)}px, ${(dy * 0.28).toFixed(2)}px)`;
					} else if (magnet.style.transform) {
						magnet.style.transform = '';
					}
				}
			},
			{ passive: true }
		);

		document.addEventListener('pointerleave', () => {
			ring?.classList.remove('is-visible');
			glow?.classList.remove('is-visible');
			for (const magnet of magnets) magnet.style.transform = '';
		});

		requestAnimationFrame(frame);
	}

	// Staggered scroll reveal for elements marked [data-reveal].
	const revealEls = document.querySelectorAll<HTMLElement>('[data-reveal]');
	if (revealEls.length) {
		const revealObserver = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry, i) => {
					if (entry.isIntersecting) {
						const el = entry.target as HTMLElement;
						el.style.transitionDelay = `${Math.min(i * 60, 240)}ms`;
						el.classList.add('is-visible');
						revealObserver.unobserve(el);
					}
				});
			},
			{ threshold: 0.12, rootMargin: '0px 0px -40px 0px' }
		);
		revealEls.forEach((el) => revealObserver.observe(el));
	}

	// Screenshot gallery: lightbox with prev/next traversal.
	const lightbox = document.getElementById('lightbox');
	const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement | null;
	const galleryImgs = Array.from(document.querySelectorAll<HTMLImageElement>('img[data-lightbox]'));

	if (lightbox && lightboxImg && galleryImgs.length) {
		let index = -1;
		const render = (i: number) => {
			index = (i + galleryImgs.length) % galleryImgs.length;
			const img = galleryImgs[index];
			lightboxImg.src = img.currentSrc || img.src;
			lightboxImg.alt = img.alt;
		};
		const open = (i: number) => {
			render(i);
			lightbox.classList.remove('hidden');
			lightbox.classList.add('flex');
			lightbox.setAttribute('aria-hidden', 'false');
		};
		const closeLightbox = () => {
			lightbox.classList.add('hidden');
			lightbox.classList.remove('flex');
			lightbox.setAttribute('aria-hidden', 'true');
			lightboxImg.removeAttribute('src');
			index = -1;
		};
		galleryImgs.forEach((img, i) => img.addEventListener('click', () => open(i)));
		lightboxImg.addEventListener('click', (e) => e.stopPropagation());
		lightbox.addEventListener('click', closeLightbox);
		document.getElementById('lightbox-prev')?.addEventListener('click', (e) => {
			e.stopPropagation();
			render(index - 1);
		});
		document.getElementById('lightbox-next')?.addEventListener('click', (e) => {
			e.stopPropagation();
			render(index + 1);
		});
		document.addEventListener('keydown', (e) => {
			if (lightbox.classList.contains('hidden')) return;
			if (e.key === 'Escape') closeLightbox();
			else if (e.key === 'ArrowLeft') render(index - 1);
			else if (e.key === 'ArrowRight') render(index + 1);
		});
	}

	// Filmstrip: arrow buttons scroll it; vertical wheel scrolls horizontally.
	const strip = document.getElementById('gallery-strip');
	if (strip) {
		const step = () => Math.max(strip.clientWidth * 0.8, 220);
		document.getElementById('gallery-prev')?.addEventListener('click', () =>
			strip.scrollBy({ left: -step(), behavior: 'smooth' })
		);
		document.getElementById('gallery-next')?.addEventListener('click', () =>
			strip.scrollBy({ left: step(), behavior: 'smooth' })
		);
		strip.addEventListener(
			'wheel',
			(e) => {
				if (Math.abs(e.deltaY) > Math.abs(e.deltaX)) {
					strip.scrollLeft += e.deltaY;
					e.preventDefault();
				}
			},
			{ passive: false }
		);
	}
</script>

<style is:global>
	/* View Transitions API styling for smooth theme changes */
	::view-transition-old(root),
	::view-transition-new(root) {
		animation: none;
		mix-blend-mode: normal;
	}

	::view-transition-old(root) {
		z-index: 1;
	}

	::view-transition-new(root) {
		z-index: 999;
	}

	/* Fallback for browsers that don't support View Transitions */
	@supports not selector(::view-transition-group(*)) {
		html {
			transition: background-color 0.5s ease-in-out, color 0.5s ease-in-out;
		}
		
		* {
			transition: background-color 0.3s ease-in-out, 
						color 0.3s ease-in-out, 
						border-color 0.3s ease-in-out,
						box-shadow 0.3s ease-in-out;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		::view-transition-old(root),
		::view-transition-new(root) {
			animation: none !important;
		}
		
		* {
			transition: none !important;
		}
	}

	/* Custom scrollbar for that raw engineering feel */
	::-webkit-scrollbar {
		width: 8px;
	}
	::-webkit-scrollbar-track {
		background: #0f0f10;
	}
	::-webkit-scrollbar-thumb {
		background: #27272a;
		border-radius: 4px;
	}
	::-webkit-scrollbar-thumb:hover {
		background: #3f3f46;
	}
</style>